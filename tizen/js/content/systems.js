'use strict';
var NG = typeof window !== 'undefined' ? (window.NG = window.NG || {}) : (globalThis.NG = globalThis.NG || {});
NG.CONCEPTS = NG.CONCEPTS || [];
NG.CONCEPTS.push(
  { tag:'Systems', title:'Blast radius', body:'Before asking whether a component can fail, ask what else burns when it does. Good architecture is mostly the art of <em>drawing smaller circles</em>.' },
  { tag:'Systems', title:'Time to live', body:'Nothing should be permanent by default. Access, records, automations — give each a clock, and let expiry do the housekeeping that humans forget.' },
  { tag:'Systems', title:'Idempotency', body:'An operation you can safely run twice is an operation you can safely retry. Most reliability engineering is just this idea, wearing different hats.' },
  { tag:'Systems', title:'Least privilege', body:'Grant the minimum needed to do the job, for the minimum time needed to do it. Not because people are untrustworthy — because <em>credentials leak</em>.' },
  { tag:'Systems', title:'Backpressure', body:'When a system is overwhelmed, the healthy response is to slow the sender, not to buffer in silence. Queues that only ever grow are outages <em>scheduled for later</em>.' },
  { tag:'Systems', title:'The circuit breaker', body:'A service that keeps hammering a dying dependency finishes the job. After repeated failures, trip open, fail fast, and only <em>probe</em> for recovery — the electrical part, translated into software.' },
  { tag:'Systems', title:'Bulkheads', body:'Ships survive holes because compartments flood alone. Give each tenant, feature and thread pool its own compartment, and one failure stays a leak instead of a sinking.' },
  { tag:'Systems', title:'The thundering herd', body:'One cache entry expires, and a thousand waiting clients charge the database at once. Stagger the timers — synchronized recovery is just a second outage.' },
  { tag:'Systems', title:'The CAP trade-off', body:'A network partition is not optional — it will happen. The real choice a distributed system makes is what to do when it does: answer with data that might be stale, or <em>refuse to answer at all</em>.' },
  { tag:'Systems', title:'Eventual consistency', body:'Every replica will agree eventually, once the writes stop arriving and the gossip catches up. The system promises not that it is always right, only that it will <em>stop being wrong</em>.' },
  { tag:'Systems', title:'The two generals problem', body:'Two armies must attack in unison, coordinating only by messenger through hostile territory — and no number of confirmations ever proves the last one arrived. A proof that <em>perfect agreement</em> over an unreliable channel is simply impossible.' },
  { tag:'Systems', title:'Exponential backoff, with jitter', body:'Doubling the wait after each failed retry eases the pressure — but if every client doubles on the same clock, they collide again in lockstep. A little randomness in the delay is what actually breaks the rhythm.' },
  { tag:'Systems', title:'Hot paths', body:'Most of a system’s time is spent in a small fraction of its code, run over and over under load. Optimize there first — a faster cold path is a rounding error nobody will notice.' },
  { tag:'Systems', title:'Tail latency', body:'The slowest one percent of requests is where trust quietly erodes, even while the dashboard reports a healthy average. A page built from a hundred parallel calls is only as fast as its <em>slowest straggler</em>.' },
  { tag:'Systems', title:'p99 versus average', body:'The average tells you how the typical request felt; the 99th percentile tells you how the unluckiest one in a hundred felt. At scale, someone is always living in the tail.' },
  { tag:'Systems', title:'Graceful degradation', body:'When a recommendation engine dies, showing a generic bestseller list beats showing nothing. A system that <em>sheds its least essential features</em> under load survives; one that offers everything or nothing usually offers nothing.' },
  { tag:'Systems', title:'Feature flags', body:'A flag lets new code ship dark and go live with a toggle instead of a deploy — and turn back off just as fast. Rollback becomes a switch, not a scramble.' },
  { tag:'Systems', title:'Blue-green deploys', body:'Two identical production environments, only one live at a time — the new version warms up in the shadow of the old one before traffic ever touches it. Rollback is a router change, not a rebuild.' },
  { tag:'Systems', title:'The strangler fig', body:'Named for the vine that slowly envelops a host tree until the original trunk can be removed entirely. Rewrite a legacy system the same way — route <em>one piece at a time</em> to the new code until nothing old remains.' },
  { tag:'Systems', title:'Conway’s law', body:'Organizations design systems that copy their own communication structure — four teams building a compiler will build a four-pass compiler. Draw the org chart first, and you have basically drawn the architecture diagram.' },
  { tag:'Systems', title:'Technical debt', body:'A shortcut taken today is a loan against tomorrow’s time, and like any loan it accrues interest — the codebase gets harder to change the longer the debt goes unpaid. Some debt is worth taking; all of it eventually <em>comes due</em>.' },
  { tag:'Systems', title:'Caching hierarchies', body:'Each layer trades size for speed — a small fast cache backed by a bigger slower one, backed by the slowest, biggest store of all. The trick is never caching the one thing that has to be true right now.' },
  { tag:'Systems', title:'The write-ahead log', body:'Before a database changes anything, it writes down what it is about to do — so that after a crash, it can replay the log and finish the job it started. Durability is mostly just <em>writing it down first</em>.' },
  { tag:'Systems', title:'Event sourcing', body:'Instead of storing the current state, store every change that ever led to it — the present becomes a <em>replay</em>, not a record. Nothing is ever overwritten; the full history is the database.' },
  { tag:'Systems', title:'The outbox pattern', body:'Writing to a database and publishing a message are two separate actions, and a crash between them loses one or duplicates the other. Write both to the same table in the same transaction, and let a separate process do the publishing.' },
  { tag:'Systems', title:'Sagas', body:'A transaction that spans several services cannot simply roll back — there is no single lock to release. A saga instead runs a chain of local steps, each with its own compensating action if a later one fails.' },
  { tag:'Systems', title:'Dead letter queues', body:'A message that fails processing five times in a row is not going to succeed on the sixth. Set it aside in its own queue for a human to inspect, instead of retrying it forever or dropping it silently.' },
  { tag:'Systems', title:'Chaos engineering', body:'Netflix built Chaos Monkey to kill production servers at random during business hours, <em>on purpose</em>, to prove the system could survive the failures it claimed to tolerate. Confidence untested is just a guess in a nice diagram.' },
  { tag:'Systems', title:'Error budgets', body:'A target of 99.9 percent uptime is also a permit to fail 0.1 percent of the time — and that permitted failure is a budget to spend on releases, experiments and risk. Perfect reliability is not the goal; agreed reliability is.' },
  { tag:'Systems', title:'The four golden signals', body:'Latency, traffic, errors and saturation — Google’s short list of what to watch if you can only watch four things. Most incidents announce themselves in at least one of the four before anyone opens a ticket.' },
  { tag:'Systems', title:'Runbooks', body:'A runbook is the checklist written at 3pm for the version of you who gets paged at 3am — calm, specific, and <em>too tired to improvise</em>. The best ones are tested before they are needed.' },
  { tag:'Systems', title:'The blameless post-mortem', body:'The question after an outage is never who broke it, but what let the mistake reach production undetected. Blame teaches people to hide problems; a blameless review is the only kind that actually surfaces them.' },
  { tag:'Systems', title:'The pager as design feedback', body:'A system that pages someone at 3am for the same fault every month is not badly staffed — it is <em>badly designed</em>. The fix belongs in the architecture, not in the on-call rotation.' },
  { tag:'Systems', title:'The N+1 query', body:'Fetch a list of a hundred orders, then loop and fetch each customer separately, and one page load just became a hundred and one round trips to the database. The fix is almost always to ask for it all at once.' },
  { tag:'Systems', title:'Connection pooling', body:'Opening a fresh database connection for every request is like renting a car for every errand. A pool keeps a handful of connections warm and hands them out and back, and the handshake cost disappears.' },
  { tag:'Systems', title:'Race conditions', body:'Two operations read the same balance, both compute a new total, and both write it back — <em>one update vanishes</em> without a single error being raised. The bug only appears when the timing is unlucky, which is what makes it so hard to catch.' },
  { tag:'Systems', title:'Deadlock’s four conditions', body:'Mutual exclusion, holding a resource while waiting for another, no way to force a release, and a circular chain of waiters — deadlock needs all four at once. Break any single one, and the standoff cannot form.' },
  { tag:'Systems', title:'The CAP of teams', body:'A siloed team can move fast on its own data, or stay perfectly aligned with everyone else’s — rarely both at once, especially under deadline pressure. Organizations hit the same trade-off distributed databases do, just measured in meetings instead of milliseconds.' },
  { tag:'Systems', title:'YAGNI', body:'“You aren’t gonna need it” — the case against building the configurable, extensible, future-proofed version of a feature nobody has asked for. Flexibility has a carrying cost even when it sits unused.' },
  { tag:'Systems', title:'The second-system effect', body:'Fred Brooks noticed that the second system an engineer builds is always the most dangerous — chastened by the first one’s limits, they cram in every feature the first system lacked. The result is usually slower and later than either extreme.' },
  { tag:'Systems', title:'Bus factor', body:'The number of people who could be hit by a bus before a project stalls completely. A bus factor of one is not a compliment to that one person — it is a <em>single point of failure</em> with a name and a face.' },
  { tag:'Systems', title:'Bit rot', body:'Code that nobody has touched in years does not corrode on disk — it drifts out of sync with everything around it, until the day it will not even compile. Software needs maintenance the way a building needs upkeep, just to stand still.' },
  { tag:'Systems', title:'The network is reliable', body:'Packets get dropped, connections reset, cables get unplugged by a cleaner’s vacuum — the first and most persistent of the fallacies of distributed computing. Code that assumes a fixed network fails the first day it meets a real one.' },
  { tag:'Systems', title:'Latency is zero', body:'A function call and a network call look identical in the code, and that similarity is the trap — one takes nanoseconds, the other can take a stubborn, unpredictable while. Treat every remote call as <em>slow until proven otherwise</em>.' },
  { tag:'Systems', title:'Bandwidth is infinite', body:'A design that ships a full record over the wire to change one field works fine in a demo, on a fast link, with three users. At scale, every unnecessary byte is a bill that eventually arrives.' },
  { tag:'Systems', title:'The network is secure', body:'Every hop between two services is a place data can be read or altered by something other than the intended recipient. Assuming the wire is safe by default is how internal traffic ends up carrying secrets in the clear.' },
  { tag:'Systems', title:'Topology doesn’t change', body:'A hard-coded server address survives right up until the day that server is replaced, rebooted somewhere else, or scaled to ten instances instead of one. The map of a distributed system is never <em>the territory</em> for long.' },
  { tag:'Systems', title:'There is one administrator', body:'A design built for a single team with full control quietly assumes every dependency behaves the way that team expects. In practice, systems span organizations, vendors and time zones, and no one person can see the whole picture.' },
  { tag:'Systems', title:'Transport cost is zero', body:'Serializing a message, encrypting it, sending it and parsing it on the other end all cost real machine time, even before the network itself gets involved. Every remote call carries overhead that a local one never pays.' },
  { tag:'Systems', title:'The network is homogeneous', body:'Not every hop runs the same protocol, the same hardware or the same version of anything — packets cross routers, proxies and translation layers built by people who never spoke to each other. Distributed systems are assembled, not designed as one piece.' }
);
